# Password to access the Binge UI
BINGE_PASSWORD=your-strong-password-here

# Secret for signing session cookies — use a long random string
# Generate with: openssl rand -hex 32
SESSION_SECRET=your-random-32-char-secret-here

# Where torrents are saved on disk
DOWNLOAD_PATH=/data/downloads

# HTTP port
PORT=3000

NODE_ENV=production
